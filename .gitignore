# Node / Vite
app/node_modules
app/dist

# Segredos locais -- nunca versionar
.env
app/.env
app/.env.*
!app/.env.example

# Supabase CLI local
supabase/.branches
supabase/.temp

# Migracao -- fora do git inteira: os scripts tem descricoes reais da
# planilha (nomes de compras, de quem dividiu conta, etc.) e o repo e
# publico. So existe no disco local, nao precisa ser versionada.
migracao/

# Editor / SO
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json
.idea
.DS_Store
